# ===== Telegram =====
BOT_TOKEN=

# ===== Admin / email routing =====
ADMIN_EMAIL=
FROM_EMAIL=

# ===== SMTP (Timeweb, 465/SSL) =====
SMTP_HOST=smtp.timeweb.ru
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=

# ===== Database / storage =====
DB_URL=sqlite:///./data/app.db
MEDIA_DIR=./data/photos

# ===== Optional PII encryption (Fernet) =====
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
PII_FERNET_KEY=

# ===== Web basic auth (admin mini-panel) =====
ADMIN_PANEL_USER=admin
ADMIN_PANEL_PASS=change_me
